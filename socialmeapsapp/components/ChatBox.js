function ChatBox() {
  return (
  <div className="flex flex-col m-2 p-3 w-1/3 h-max bg-white rounded-lg shadow">
      This is what a chat box will look like and if we add a ton of text to see if it will overflow or wrap properly it will look like this
  </div>
  )}

export default ChatBox
